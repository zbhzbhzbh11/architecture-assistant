# 演示输入用例

> 版本: 2.0
> 日期: 2026-05-15
> 用途: 答辩 5 分钟演示的输入材料 + 备选展示
> 原则: 预期结果基于规则引擎评分公式的确定性分析；LLM 投票可能导致 tie-break 微调

---

## 评分公式速查

在阅读每个用例的预期结果之前，先回顾系统的评分规则：

```
总分 = 标签基础分 (tag ∈ features → +2/项)
     + 学习权重分 (≥2次确认的 feature-style 关联 → +1/项)
     + 特定规则加成 (6条硬编码规则 → +1/条)

特定规则:
  Event-Driven + high_concurrency                   → +1
  Microservices + team_size_large                   → +1
  Layered + strict_consistency                      → +1
  Pipeline-Filter + data_intensive + real_time      → +1
  CQRS + data_intensive + high_concurrency          → +1
  Microservices + high_concurrency + strict_consistency → +1

主流保底:
  Layered / Microservices / Event-Driven 始终出现在 Top 3 中
```

> **注意**: 以下计算假设学习权重为初始状态（全部为 0）。如果已累积反馈权重，评分可能略有不同。

---

## 用例 1: 即时通讯系统 ⭐ 主演示

### 输入

```
开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠，后续扩展视频通话。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| high_concurrency | ✅ | 万人 |
| real_time | ✅ | 实时、消息（在上下文中的实时语义） |
| reliability | ✅ | 可靠 |
| scalability | ✅ | 扩展 |

### 预期推荐架构

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Event-Driven Architecture** | high_concurrency(+2) + real_time(+2) + scalability(+2) + reliability(+2) + 特定规则(+1) = **9** |
| 2 | Microservices | high_concurrency(+2) + scalability(+2) + reliability(+2) = **6** |
| 3 | Layered Architecture | scalability(+2) + reliability(+2) = **4**（主流保底确保出现） |

> 注: Space-Based Architecture 理论上得分 6（high_concurrency+2, scalability+2, real_time+2），与 Microservices 并列。主流保底策略确保 Microservices 和 Layered 进入 Top 3。

### 预期组合推荐

**Microservices + Event-Driven Architecture**（复杂度 1）

微服务负责服务边界和独立部署，事件驱动解耦服务间通信——特别适合高并发、实时性要求高的 IM 场景。

### 是否触发重构建议

**否**。输入描述的是新系统需求，不含"单体"、"遗留"、"拆分"、"耦合"等重构信号。

### 演示价值

这是**最稳健的主演示用例**。特征清晰（4 个维度）、评分差异明显（9-6-4）、组合推荐有说服力、风险建议有针对性条目（Event-Driven 是 3 种有针对性的风格之一）。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| 首选变成 Microservices | "LLM 投票给 Microservices 加了 +1 分 tie-break。Event-Driven 和 Microservices 原始规则分差距是 3 分，但如果 LLM 投票了 Microservices，它仍然是强有力的候选。这正好体现了混合推理——LLM 在规则基础上提供第二意见。" |
| Top 3 中包含 Space-Based | "Space-Based Architecture 确实是一种适合高并发实时场景的架构。它没有出现在我们预期中是因为主流保底策略让 Microservices 和 Layered 优先，但出现也不意外——它的 tags 完美匹配了这个场景的特征。" |
| 特征提取少了某维度 | "特征提取依赖关键词词典。如果某维度未被命中，可能是关键词表述不在词典中（如'视频通话'未触发 scalability）。这正是 LLM 语义补全存在的价值——补充关键词遗漏的特征。" |

---

## 用例 2: 电商平台

### 输入

```
构建电商平台，订单支付库存需要强一致事务，双11高峰要抗压，多团队并行开发。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| strict_consistency | ✅ | 强一致、事务 |
| high_concurrency | ✅ | 高峰、抗压 |
| scalability | ✅ | 抗压（推断性命中，取决于词典覆盖） |
| team_size_large | ✅ | 多团队、并行开发 |

### 预期推荐架构

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Microservices** | team_size_large(+2) + high_concurrency(+2) + scalability(+2) + 特定规则(team_size_large+1) + 特定规则(high_concurrency+strict_consistency+1) = **8** |
| 2 | Event-Driven Architecture | high_concurrency(+2) + scalability(+2) + 特定规则(+1) = **5** |
| 3 | Layered Architecture | strict_consistency(+2) + scalability(+2) + 特定规则(strict_consistency+1) = **5** |

> 注: CQRS 也可能得 4-6 分（取决于 strict_consistency + high_concurrency 组合是否命中 CQRS 特定规则）。如果 CQRS 得分高于 5，它可能进入 Top 3。

### 预期组合推荐

**Microservices + Event-Driven Architecture**（复杂度 1）

微服务满足多团队并行开发和独立部署需求，事件驱动满足高并发下的异步解耦需求。双 11 场景特别适合这种组合。

### 是否触发重构建议

**否**。虽然描述了高性能要求，但输入是新系统构建而非遗留系统改造。

### 演示价值

展示**多特征维度同时命中**时的评分演算——特别是两条特定规则同时触发（team_size_large +1 和 high_concurrency+strict_consistency +1）。适合作为"如果我换一个需求，系统会怎样响应"的快速演示。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| CQRS 进入 Top 3 | "CQRS 的 tags 包含 strict_consistency、high_concurrency 和 data_intensive。电商场景有数据密集特征（订单数据），CQRS 出现在候选列表是合理的——它特别适合读写分离的高并发场景。" |
| scalability 未被命中 | "scalability 的关键词词典包含'扩展'、'弹性'等，但不一定包含'抗压'。如果未被命中，这是关键词匹配的覆盖盲区，LLM 语义补全可以填补。" |

---

## 用例 3: 银行核心账务系统

### 输入

```
银行核心账务系统，要求高可靠和强一致，审计可追溯。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| reliability | ✅ | 高可靠 |
| strict_consistency | ✅ | 强一致 |
| security | ✅ | 审计 |

### 预期推荐架构

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Layered Architecture** | strict_consistency(+2) + reliability(+2) + security(+2) + 特定规则(strict_consistency+1) = **7** |
| 2 | Microservices | reliability(+2) + security(+2) = **4** |
| 3 | Event-Driven Architecture | reliability(+2) = **2**（主流保底确保出现） |

> 注: Client-Server 得分约 4（reliability+2, security+2），与 Microservices 并列。推荐优先级取决于排序时的稳定策略。

### 预期组合推荐

**Layered Architecture + CQRS**（复杂度 1）

分层架构满足账务系统的业务逻辑组织需求，CQRS 满足查询（审计追溯）和写入（交易处理）的分离需求。

### 是否触发重构建议

**否**。

### 演示价值

展示**强一致性 + 安全合规**场景——这是 Layered Architecture 最经典的适用场景。与用例 1（Event-Driven 主导）形成鲜明对比，展示系统对不同特征组合的差异化推荐能力。同时展示了 security 维度的关键词"审计"触发。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| Microservices 排第一 | "Microservices 的 reliability 和 security 标签各 +2 分，加上可能的 LLM 投票加分，在某些条件下可能超过 Layered。但两者差距很小——说明对于这种特征组合，两种架构都有合理的一面。" |
| security 命中覆盖不足 | "security 维度的关键词词典包含'加密'、'认证'、'鉴权'、'审计'、'合规'、'脱敏'等 15 个词。'审计'是确定性的命中。" |

---

## 用例 4: 日志分析平台

### 输入

```
日志分析平台，每秒采集百万条日志并实时告警。
```

### 触发特征（LLM 已配置时）

| 维度 | 命中 | 来源 |
|------|------|------|
| real_time | ✅ | 规则 — "实时"命中词典 |
| data_intensive | ✅ | 规则 — "日志"命中词典 |
| high_concurrency | ✅ | **LLM 补全** — "每秒采集百万条"语义推断 |

> 规则引擎命中 2 维（≤2），触发 LLM 语义补全。LLM 从"每秒"和"百万"推断出高并发需求。**如果 LLM 未配置**，high_concurrency 不会命中，结果走下方的"LLM 不可用时"情况。

### 推荐结果（LLM 已配置 — 实测）

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Event-Driven Architecture** | high_concurrency(+2) + real_time(+2) + data_intensive(+2) + 特定规则(+1) = **7** |
| 2 | Pipeline-Filter | data_intensive(+2) + real_time(+2) + 特定规则(data_intensive+real_time+1) = **5** |
| 3 | Microservices | high_concurrency(+2) = **2**（主流保底） |

### LLM 不可用时（对比参考）

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Pipeline-Filter** | data_intensive(+2) + real_time(+2) + 特定规则(+1) = **5** |
| 2 | Event-Driven Architecture | real_time(+2) + data_intensive(+2) = **4** |
| 3 | Microservices | 0（主流保底基线） |

> 两种情况都合理——LLM 补全了 high_concurrency 后 Event-Driven 胜出，否则 Pipeline-Filter 胜出。两种推荐都有工程合理性。

### 预期组合推荐

**Pipeline-Filter + Event-Driven Architecture**（复杂度 2）。

### 是否触发重构建议

**否**。

### 演示价值

**规则+LLM 协同的最佳展示用例**。规则引擎命中 2 维触发 LLM 补全阈值（≤2），LLM 从"每秒"和"百万"中推断出了高并发——这正是词典覆盖盲区的典型场景。如果 Neo4j 已初始化，图谱证据还可展示 Pipeline-Filter 和 Event-Driven 的 COMPLEMENTS 关系。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| Pipeline-Filter 是首选 | "LLM 未配置或补全调用失败，系统走了纯规则模式。high_concurrency 未被命中，Pipeline-Filter 凭借 data_intensive+real_time 特定规则胜出。两种结果都合理——这恰好对比了纯规则 vs 规则+LLM 两种模式。" |
| high_concurrency 标注 llm_supplement | "这正是系统设计的亮点——规则词典覆盖不到的表达，LLM 通过语义理解补全。'每秒百万'不是关键词词典里的高并发词，但 LLM 理解了它的含义。同时来源标注为 llm_supplement，让评审者知道这不是规则确定性产出。" |
| 图谱证据区显示"无图谱证据" | "Neo4j 未启动或未初始化，系统自动回退 JSON 后端。" |

---

## 用例 5: 旧单体系统重构 ⭐ 重构演示

### 输入

```
已有单体电商系统，订单库存耦合严重，性能瓶颈明显，希望拆分为微服务。
```

### 触发特征（LLM 已配置时）

| 维度 | 命中 | 来源 |
|------|------|------|
| scalability | ✅ | **LLM 补全** — "拆分"、"性能瓶颈"语义推断可扩展需求 |
| team_size_large | ✅ | **LLM 补全** — "拆分为微服务"隐含多团队并行开发 |

> 规则引擎从"拆分"、"单体"、"耦合"等词中可能命中 0 维。"拆分"不在 scalability 词典中（词典以"扩展"、"弹性"为主），"单体"也不在 team 词典中。命中 0 维（≤2），触发 LLM 语义补全后补充了 2 维。
>
> **如果 LLM 未配置**：特征命中接近 0，系统走主流保底策略，三种主流架构各得 0 分按稳定排序出现在 Top 3。重构建议仍然正常产出（refactoring-agent 有独立的坏味词典）。

### 推荐结果（LLM 已配置 — 实测）

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Microservices** | scalability(+2) + team_size_large(+2) + 特定规则(team_size_large+1) = **5** |
| 2 | Event-Driven Architecture | scalability(+2) = **2**（主流保底） |
| 3 | Layered Architecture | scalability(+2) = **2**（主流保底） |

### LLM 不可用时（对比参考）

| 排名 | 风格 | 评分演算 |
|------|------|---------|
| **1** | **Layered Architecture** | 0（主流保底，按稳定排序） |
| 2 | Microservices | 0（主流保底） |
| 3 | Event-Driven Architecture | 0（主流保底） |

### 预期重构建议

| 字段 | 预期值 |
|------|--------|
| refactoring_needed | ✅ true |
| detected_architecture_smells | monolith_coupling（单体耦合）、scaling_bottleneck（扩展瓶颈） |
| suggested_patterns | Strangler Fig（绞杀者模式）或 Anti-Corruption Layer（防腐层） |
| migration_steps | ≥ 3 个阶段的渐进式迁移步骤 |
| risks | 含风险提示 |
| mitigation_suggestions | 含缓解措施 |

### 是否触发重构建议

**是**。输入中的"单体"、"耦合"、"拆分"触发 refactoring-agent 的坏味检测。重构信号与特征信号是**两套独立词典**——即使特征命中为 0，重构建议仍可正常产出。

### 演示价值

**双重价值**：(1) 展示 LLM 语义补全在特征极稀疏（0 维）输入上的工作——这是 ≤2 阈值触发的最佳案例。(2) 展示架构推荐（微服务）与重构建议（绞杀者模式）的协同——推荐的目标架构恰好是重构的迁移方向。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| Top 1 是 Layered | "LLM 未配置或补全调用失败。特征信号为 0 时，主流保底策略接管——Layered、Microservices、Event-Driven 按稳定排序并列出现。这不是 bug，是保底策略确保任何输入都有可参考的候选基线。" |
| scalability/team 标注 llm_supplement | "'拆分'不在 scalability 词典中——词典以'扩展'、'弹性'为主。LLM 理解了'拆分→微服务'隐含的可扩展和多团队需求。来源标注让评审者清楚区分规则确定性产出和 LLM 推断。" |
| 重构建议未触发 | "refactoring-agent 的坏味检测基于独立的关键词词典。如果'单体'、'耦合'未被其词典覆盖，可能判定 refactoring_needed=false。已记录为改进方向。" |

---

## 用例 6: 在线教育平台

### 输入

```
在线教育平台，支持万人同时听课，课程直播低延迟，互动消息及时推送。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| high_concurrency | ✅ | 万人、同时 |
| real_time | ✅ | 直播、低延迟、及时推送 |

> 注: 特征命中较少（2 维）。如果 LLM 已配置，可能触发语义补全，补充 scalability 和 reliability。

### 预期推荐架构

| 排名 | 风格 | 评分演算（无 LLM 补全） |
|------|------|---------|
| **1** | **Event-Driven Architecture** | high_concurrency(+2) + real_time(+2) + 特定规则(+1) = **5** |
| 2 | Microservices | high_concurrency(+2) = **2**（主流保底） |
| 3 | Layered Architecture | 无标签命中 = **0**（主流保底基线） |

> 注: 当特征稀疏时，Top 2 和 Top 3 的分数差距较大——这恰好展示了主流保底策略的价值：即使只有 Event-Driven 明显合适，评委也能看到另外两种主流架构作为对比基线。

### 预期组合推荐

**Microservices + Event-Driven Architecture**（复杂度 1）

教育平台的课程管理（微服务边界）和实时互动（事件驱动）是天然的组合场景。

### 是否触发重构建议

**否**。

### 演示价值

展示**特征稀疏场景**下的系统行为——当只有 2 个维度命中时，评分差异更加鲜明。这也是展示 LLM 语义补全价值的场景——如果 LLM 已配置，可以看到补全后的特征数量和补全前的差异。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| 特征补全后推荐变化 | "LLM 补全可能新增 scalability 特征——这会提升 Microservices 和 Layered 的得分。这是 LLM 语义理解的正面价值：从'万人同时听课'推断出可扩展性需求。" |
| Layered 得 0 分 | "Layered 在这个场景确实不是最优选择——但我们的主流保底策略确保它出现在候选列表中，让用户有一个明确的对比基线：Event-Driven 5 分 vs Layered 0 分，解释为什么分层架构不适合这个场景。" |

---

## 用例 7: 医疗影像处理系统

### 输入

```
医疗影像处理系统，图像处理链路长，适合分阶段流水线，患者数据需要隐私保护和合规审计。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| data_intensive | ✅ | 图像处理、影像 |
| security | ✅ | 隐私保护、合规审计 |
| complex_business | ✅ | 分阶段（可能触发，取决于词典覆盖） |

> 注: "流水线"和"分阶段"可能触发 Pipeline-Filter 相关的特征推理。"隐私保护"和"合规审计"明确触发 security。

### 预期推荐架构

**可能推荐** (特征覆盖稀疏，优先级依赖主流保底策略):

| 风格 | 概率 | 理由 |
|------|------|------|
| **Event-Driven Architecture** | 高 | data_intensive(+2)，主流保底 |
| Pipeline-Filter | 中 | data_intensive(+2)，但不一定进入 Top 3（被主流保底挤掉） |
| Layered Architecture | 中 | security(+2)，主流保底 |
| Microservices | 中 | security(+2)，主流保底 |

> 注: 这是推荐结果最不确定的用例之一——特征相对稀疏且没有触发强烈的特定规则。**不建议作为主演示用例**，但适合 Q&A 环节作为"边界案例"展示。

### 预期组合推荐

如果有意义，可能推荐 **Pipeline-Filter + Event-Driven Architecture**。但可能性较低——取决于特征命中是否足够。

### 是否触发重构建议

**否**。

### 演示价值

展示**系统在特征稀疏时的诚实表现**——不会强行给出高分推荐，而是通过主流保底策略给出一个"虽然没有明显最佳选择，但三种主流架构可供参考"的对比基线。适合回答"如果用户输入很模糊怎么办"的提问。

### 如果结果偏差，现场怎么解释

> "医疗影像场景的特征信号相对稀疏——'流水线'确实暗示了 Pipeline-Filter，但如果不命中 data_intensive 词典中的关键词，评分会比较保守。这体现了系统的设计原则——宁可给出保守的候选列表，也不强行推荐不合适的架构。LLM 语义补全可以增强对隐含特征的识别——如果 LLM 已配置，可以展示补全前后的差异。"

---

## 用例 8: 企业数据中台

### 输入

```
企业数据中台，ETL任务复杂，要求可扩展和可观测，支持批处理和流处理，多数据源接入。
```

### 预期触发特征

| 维度 | 命中 | 关键词证据 |
|------|------|-----------|
| data_intensive | ✅ | ETL、数据中台、批处理、流处理、数据源 |
| scalability | ✅ | 可扩展 |
| complex_business | ✅ | 数据中台（可能命中复杂业务词典） |

> 注: 这是 data_intensive 特征信号最强的用例之一——"ETL"、"批处理"、"流处理"、"数据源"四个关键词密集命中。

### 预期推荐架构

**可能推荐**:

| 风格 | 概率 | 理由 |
|------|------|------|
| **Event-Driven Architecture** | 高 | data_intensive(+2) + scalability(+2) = 4，主流保底 |
| Pipeline-Filter | 中-高 | data_intensive(+2) + scalability(+2) = 4，但不一定进入 Top 3 |
| Microservices | 中 | scalability(+2)，主流保底 |
| Layered Architecture | 中 | complex_business(+2) + scalability(+2) = 4（如果 complex_business 命中） |

### 预期组合推荐

**Pipeline-Filter + Event-Driven Architecture**（复杂度 2）

管道-过滤器处理 ETL 流水线，事件驱动处理流处理场景的异步事件。复杂度 2 反映了同时管理批处理和流处理管道的工程成本。

### 是否触发重构建议

**否**。

### 演示价值

展示**数据密集型场景的最强信号**。"ETL"、"批处理"、"流处理"、"数据源"密集命中 data_intensive 词典——展示特征提取的词典覆盖能力。同时也是 Pipeline-Filter + Event-Driven 组合推荐的最佳场景之一。

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| Pipeline-Filter 不在 Top 3 | "Pipeline-Filter 得分约 4-5 分，但如果 Layered 也被 complex_business 和 scalability 各 +2，它与 Pipeline-Filter 分数持平。主流保底策略优先确保 Layered 出现在候选列表中。这是主流保底的代价——可能挤掉得分相当的非主流选择。我们设计文档中已记录了这一点。" |
| complex_business 未被命中 | "'数据中台'是一个相对新的概念，可能不在 complex_business 词典中。这展示了关键词词典的覆盖局限性——也是 LLM 语义补全存在的价值。" |

---

## 用例总览对比表

| # | 场景 | 核心特征 | 首选推荐 | 重构触发 | 演示优先级 |
|---|------|---------|---------|---------|-----------|
| 1 | IM 系统 | 高并发+实时+可靠+扩展 | Event-Driven | 否 | ⭐ 主演示 |
| 2 | 电商平台 | 强一致+高并发+多团队 | Microservices | 否 | 备选 |
| 3 | 银行账务 | 强一致+可靠+安全 | Layered | 否 | 备选 |
| 4 | 日志分析 | 实时+数据密集(规则) +高并发(LLM补全) | Event-Driven | 否 | ⭐ 规则+LLM协同 + 图谱证据 |
| 5 | 旧单体重构 | 扩展+多团队(均LLM补全) | Microservices | **是** | ⭐ 重构演示 + LLM零特征补全 |
| 6 | 在线教育 | 高并发+实时(稀疏) | Event-Driven | 否 | LLM 补全展示 |
| 7 | 医疗影像 | 数据密集+安全(稀疏) | 不确定 | 否 | 边界案例 |
| 8 | 数据中台 | 数据密集+扩展+复杂业务 | Event-Driven | 否 | 词典覆盖展示 |
| 9 | 知识进化 | 高并发+实时（跨领域累积） | Event-Driven（分数逐步上涨） | 否 | ⭐ 反馈→权重→跨领域迁移闭环 |

---

## 用例 9: 知识进化 — 跨领域特征学习 ⭐ 核心演示

### 演示价值

展示知识进化的完整闭环——**不是简单的"相同输入重复确认"，而是按特征维度跨领域学习**。系统从不同领域的反馈中抽取通用架构原则（"高并发→事件驱动"），与领域标签无关。

### 前提条件

- Docker 全部启动，Neo4j 已初始化
- 如果之前有残留权重数据，点击前端 **[重置]** 按钮清空（调用 `POST /feedback/reset` → 删除 Neo4j Feedback 节点 + 清空 `learned_weights.json`）
- 或执行 `docker compose down -v && docker compose up --build -d && docker compose exec knowledge-base python init/init_neo4j.py` 从零开始

### 演示步骤（分四轮，约 120 秒）

---

#### 第1轮：建立基线（教育直播领域）

**需求输入**：
```
开发在线教育平台, 需要支持10万人同时在线观看直播, 低延迟互动消息
```

点 **开始推荐**。观察事件驱动基准分数。点 **[确认 👍]**。

**预期效果**：

| 位置 | 现象 |
|------|------|
| 推荐结果 | Event-Driven Architecture = 基准分 |
| 权重表 | 高并发→Event-Driven=1 (待确认), 实时性→Event-Driven=1 (待确认) |

> 权重确认次数 = 1，**未达 ≥2 阈值**，不会影响后续推荐的分数。

---

#### 第2轮：跨领域推动阈值（跨境支付）

**需求输入**：
```
跨境支付网关, 高峰期每秒万笔交易, 要求高吞吐和实时清算
```

点 **开始推荐** → 点 **[确认 👍]**。

**预期效果**：

| 特征 | 风格 | 确认次数 | 状态 |
|------|------|----------|------|
| 高并发 | Event-Driven | 2 | ✓生效 |
| 实时性 | Event-Driven | 2 | ✓生效 |

> 第1轮的教育直播和第2轮的跨境支付**两个完全不同的领域**，因为都命中了 `高并发` 和 `实时性` 特征，权重自然累积达到阈值。

**演示时说的话**：
> "注意——两个反馈来自完全不同的领域：在线教育和跨境支付。但它们共享了 '高并发' 和 '实时性' 两个特征。系统抽取的是一般性的架构原则，不局限于某个特定系统类型。"

---

#### 第3轮：验证跨领域迁移

先点 **清缓存重试**，再输入第一轮同样的需求：
```
开发在线教育平台, 需要支持10万人同时在线观看直播, 低延迟互动消息
```

点 **开始推荐**。

**预期效果**：

| 风格 | 第1轮分数 | 第3轮分数 | 变化 |
|------|----------|----------|------|
| Event-Driven | 基准分 | 基准分 + learned boost | **上涨** |
| 其他风格 | 不变 | 不变 | — |

> Event-Driven 分数比第1轮明显更高。教育平台没有提交过新的反馈，但**跨境支付的经验被迁移过来了**——这就是跨领域通用认知。

**演示时说的话**：
> "现在回到刚才的教育平台需求——我没有再提交教育平台的反馈，但分数变了。因为跨境支付教会了系统'高并发→事件驱动'是合理的，这个经验对所有高并发需求的系统都适用。"

---

#### 第4轮：验证特征隔离

**需求输入**：
```
政务公文审批系统, 私有化部署, 权限分级, 审计日志完整, 无需高并发
```

点 **开始推荐** → 点 **[确认 👍]**。

**预期效果**：

| 特征 | 风格 | 确认次数 | 状态 |
|------|------|----------|------|
| 部署约束 | Client-Server | 1 | (待确认) |
| 安全性 | Client-Server | 1 | (待确认) |

> 权重表新增两个独立维度。高并发/实时性的权重**不受影响**。不同特征维度之间不互相污染。

**演示时说的话**：
> "最后再看一个完全不同特征集的场景——政务系统。它的权重表新增的是安全性和部署约束维度，跟前面累积的高并发、实时性权重完全隔离。系统学到的经验是在特征维度上收敛的，不会跨维度混淆。"

---

#### 补充演示：反馈 + 需求迭代（[补充反馈重新推荐]）

展示前端另一个知识进化入口——用户在推荐结果上补充新需求，系统同时记录反馈并重新推荐。

**起始状态**：假设权重表中已有 `高并发→Event-Driven=2 ✓生效`（从前几轮累积）。

**操作**：

1. 输入：
   ```
   开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠
   ```
   点 **开始推荐** → 查看推荐结果

2. 点 **[补充反馈重新推荐]** → 弹出输入框

3. 填入补充需求：
   ```
   后期可能需要快速扩展视频通话功能
   ```
   点 **[重新推荐]**

**后台发生了什么**：

```
① POST /feedback {
    requirement: "开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠",
    recommended_style: "Event-Driven Architecture",
    user_choice: "Event-Driven Architecture",
    comment: "后期可能需要快速扩展视频通话功能"  ← 用户补充写入反馈
   }
   → 权重表: scalability→Event-Driven 是否新增取决于"扩展"关键词命中

② 清空 API 网关缓存

③ 合并需求重新推荐:
   "开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠。
    补充需求：后期可能需要快速扩展视频通话功能"
   → "扩展""视频通话"触发 scalability 特征
   → Event-Driven 分数进一步上涨 (scalability tag +2, 可能有 learned boost)
```

**预期效果**：

| 时机 | 权重表 | 推荐分数 |
|------|--------|----------|
| 提交前 | 高并发:2✓ 实时性:2✓ | Event-Driven = 基准+N |
| 补充反馈后 | 高并发:2✓ 实时性:2✓ + scalability 可能新增 | Event-Driven = 基准+N+2（tag匹配+可能的learned boost） |

**演示时说的话**：
> "最后展示一下 '补充反馈重新推荐' 功能。它和 '确认' 的区别在于：用户不仅确认了推荐正确，还可以输入补充需求。系统会把补充文本作为反馈的 comment 记录下来，再合并原需求和补充需求，清掉缓存重新推荐。用户在迭代需求的同时，也在持续训练系统。"

---

### 知识进化原理图

```
第1轮 教育直播 ~ 第2轮 跨境支付
  ├─ "万人""高并发"        ├─ "高吞吐""万笔"
  │   → high_concurrency   │   → high_concurrency ← 同一特征维度
  ├─ "实时""低延迟"        ├─ "实时清算"
  │   → real_time          │   → real_time         ← 同一特征维度
  │                         │
  └─── 权重累积 ────────────┘
          │
          ▼
    high_concurrency → Event-Driven = 2 ✓生效
    real_time         → Event-Driven = 2 ✓生效
          │
          ▼
    第3轮 重新推荐教育直播 → Event-Driven 分上涨
    (教育直播自身并未提交更多反馈，但受益于跨境支付的经验)
```

### 前端关键操作提示

| 步骤 | 按钮 | 作用 |
|------|------|------|
| 清除旧缓存 | **[清缓存重试]** | 清空 API 网关请求级缓存，确保推荐是实时计算 |
| 提交反馈 | **[确认 👍]** | `POST /feedback` → Neo4j 写入 → 即时更新权重 |
| 带补充反馈重推 | **[补充反馈重新推荐]** | 提交反馈 + 清缓存 + 合并需求重新推荐 |
| 查看权重 | **[刷新权重]** | 拉取最新 learned_weights 表格 |
| 清零权重 | **[重置]** | 删除 Neo4j Feedback 节点 + 清空 `learned_weights.json` |

### 注意事项

| 常见现象 | 原因 |
|----------|------|
| 权重表出现但分数不变 | 确认次数 = 1，未达 ≥2 阈值 |
| 权重表显示 ✓生效但分数没涨 | API 网关缓存了旧结果，点 **[清缓存重试]** |
| 重置后权重仍不归零 | `docker compose down` 未带 `-v`，Neo4j 数据卷保留旧数据 |

### 预期权重变化详表

| 轮次 | 特征 | 风格 | 确认次数 | 阈值 | 影响分数 |
|------|------|------|----------|------|----------|
| 第1轮后 | 高并发 | Event-Driven | 1 | 未达 | 无 |
| 第1轮后 | 实时性 | Event-Driven | 1 | 未达 | 无 |
| 第2轮后 | **高并发** | Event-Driven | **2** | **已达标** | **+1 分** |
| 第2轮后 | **实时性** | Event-Driven | **2** | **已达标** | **+1 分** |
| 第4轮后 | 安全性 | Client-Server | 1 | 未达 | 无 |
| 第4轮后 | 部署约束 | Client-Server | 1 | 未达 | 无 |
开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠。
```

### 演示价值

展示知识进化的完整闭环：用户确认推荐 → 权重自动累积 → 下一次同等需求推荐分数变化。这是系统区别于无状态 LLM 推荐的核心特征之一。

### 演示步骤（分三步，预计 60 秒）

**Step 1: 清空权重，记录基线分数**

```bash
# 确保从零开始
echo {} > services/knowledge_base/data/learned_weights.json
curl -s -X POST http://localhost:8000/cache/clear

# 发起推荐，查看 Event-Driven 原始分数
curl -s -X POST http://localhost:8000/api/v1/recommend -H "Content-Type: application/json" -d "{\"requirement\":\"开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠。\"}" | python -c "import sys,json; d=json.load(sys.stdin); [print(f'{c[\"style\"]}: {c[\"score\"]}分') for c in d['candidates']]"
```

预期输出（基线）：
```
Event-Driven Architecture: 7分
Microservices: 4分
Layered Architecture: 2分
```

**Step 2: 模拟用户确认反馈**

```bash
# 模拟两次确认 Event-Driven 被推荐成功
curl -s -X POST http://localhost:8004/feedback -H "Content-Type: application/json" -d "{\"requirement\":\"开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠。\",\"recommended_style\":\"Event-Driven Architecture\",\"is_confirmed\":true}"

curl -s -X POST http://localhost:8004/feedback -H "Content-Type: application/json" -d "{\"requirement\":\"即时通讯系统需要高并发实时处理能力\",\"recommended_style\":\"Event-Driven Architecture\",\"is_confirmed\":true}"

# 查看更新后的权重
curl -s http://localhost:8004/feedback/weights | python -m json.tool
```

预期 `learned_weights` 中出现：
```json
{
  "high_concurrency": {"Event-Driven Architecture": 2},
  "real_time": {"Event-Driven Architecture": 2},
  ...
}
```

**Step 3: 清缓存，重新请求，对比分数**

```bash
curl -s -X POST http://localhost:8000/cache/clear
curl -s -X POST http://localhost:8000/api/v1/recommend -H "Content-Type: application/json" -d "{\"requirement\":\"开发跨平台即时通讯系统，支持万人同时在线，消息实时可靠。\"}" | python -c "import sys,json; d=json.load(sys.stdin); [print(f'{c[\"style\"]}: {c[\"score\"]}分') for c in d['candidates']]"
```

预期输出（学习后）：
```
Event-Driven Architecture: 9分
Microservices: 4分
Layered Architecture: 2分
```

Event-Driven 从 7 分 → 9 分（high_concurrency 和 real_time 的学习权重各 +1）。其他风格分数不变。

### 演示时说的话

> "现在展示一个调用一次 LLM API 无法实现的能力——知识进化。刚才 Event-Driven 得分是 7 分。我模拟两次用户确认反馈后，系统自动更新了学习权重——high_concurrency 到 Event-Driven 的关联被确认了两次，权重生效（>=2 阈值）。重新请求同一个需求，Event-Driven 从 7 分变成了 9 分。这个 +2 来自用户对系统推荐的确认，而不是我们硬编码的规则。系统越用越准确。"

### 预期权重变化

| 特征维度 | 关联风格 | 确认次数 | 是否生效 |
|---------|---------|---------|---------|
| high_concurrency | Event-Driven Architecture | 2 | ✅（≥2，+1 分） |
| real_time | Event-Driven Architecture | 2 | ✅（≥2，+1 分） |

### 如果结果偏差，现场怎么解释

| 偏差情况 | 解释 |
|---------|------|
| 分数变化不如预期 | "权重生效阈值为 ≥2 次确认。检查 `learned_weights.json`，确认计数器是否确实达到了 2。如果只有 1 次反馈，分数不会变化——这是有意设计的防噪声机制。" |
| 其他风格分数也变了 | "确认的反馈文本中可能包含其他特征的关联——`_extract_features_from_requirement()` 通过词典从反馈文本中提取特征并累加权重。这是预期行为。" |
| 权重查询返回空 | "knowledge-base 未启动或 JSON 文件被重置。检查 `docker compose ps knowledge-base` 和 `learned_weights.json` 文件。" |

### 演示后重置

```bash
echo {} > services/knowledge_base/data/learned_weights.json
curl -s -X POST http://localhost:8000/cache/clear
```

---

## 演示路线推荐

### 路线 A: 稳健路线（推荐）

```
用例1 (IM系统) → 用例5 (旧单体重构) → 用例3 或 用例2 (快速对比)
        90秒              90秒                  60秒
```

展示顺序：基础推荐 → 重构建议 → 对比不同场景的推荐差异。3 个用例覆盖全功能路径。

### 路线 B: 深度路线

```
用例1 (IM系统) → 用例4 (日志分析) → 用例5 (旧单体重构)
        90秒              60秒                  90秒
```

展示顺序：基础推荐 → 图谱证据 + 特定组合 → 重构建议。重点展示 Neo4j 图谱和重构能力。

### 路线 C: 知识进化（加分项）

```
用例9 (知识进化) — 120 秒展示四轮跨领域特征学习：基线→跨领域推动阈值→验证迁移→特征隔离
```

演示顺序：清空权重→查基线分数→模拟反馈→查权重变化→再次请求→分数对比。

### 路线 D: 问答补充

```
用例7 (医疗影像) 或 用例6 (在线教育) — 按需在 Q&A 中展示
```

当评委问到"如果用户需求很模糊怎么办"或"特征识别不完整怎么办"时，用边界案例展示系统的诚实表现。

---

## 使用说明

1. **答辩前**: 在主演示环境上逐一测试 1-5 号用例，确认实际推荐结果与预期一致。如有偏差，用"如果结果偏差"栏的话术准备。
2. **演示中**: 用例 1 作为主演示，用例 5 跟进展示重构能力。其他用例按需在 Q&A 中使用。
3. **Q&A 中**: 如被问到特征识别能力、系统局限、或"换个场景试试"，用用例 3（银行账务）和用例 6（在线教育）快速对比。
4. **不要一次演示所有用例**——5 分钟演示时间只够 2-3 个。

---

*本文档所有评分演算均基于 `matching_agent/app/main.py:score_style()` 的实际逻辑。标注"可能推荐"的用例表示 LLM 投票或主流保底策略可能导致预期之外的结果。学习权重假设为初始空状态。*
